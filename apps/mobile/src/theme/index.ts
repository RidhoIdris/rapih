/**
 * Theme barrel. Import everything design-related from `@/theme`:
 *
 *   import { palette, space, radius, shadow, textVariants } from '@/theme';
 */
export { palette, space, radius, shadow } from './tokens';
export type { ColorToken } from './tokens';
export { fontFamily, textVariants } from './typography';
export type { TextVariant } from './typography';
